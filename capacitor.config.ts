import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simapro.app',        // Bisa kamu ubah sesuai domain/app ID unikmu
  appName: 'SIMAPRO',              // Ini yang tampil sebagai nama aplikasi
  webDir: 'dist'
};

export default config;
